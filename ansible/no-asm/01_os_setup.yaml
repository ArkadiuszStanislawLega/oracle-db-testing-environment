---
- name: Zero to DB - Kompletny Provisioning Oracle 19c SE2 - no ASM
  hosts: all
  become: yes
  gather_facts: no

  pre_tasks:
    - name: Bootstrap Python 3.9
      raw: yum install -y python39
      changed_when: false

    - name: Ręczne zebranie faktów systemowych
      setup:

  vars:
    ansible_python_interpreter: /usr/bin/python3.9
    dir_ora_inventory: "/u01/app/oraInventory"
    dir_oracle_base: "/u01/app/oracle"
    dir_db_home: "/u01/app/oracle/product/19.3.0/dbhome_1"
    
    dir_bin: "/u01"
    dir_u02_base: "/u02/app/oracle"
    dir_db_logs: "/u02"
    dir_db_mirr: "/u03"
    dir_db_backup: "/u04"
    dir_pdb1: "/u05"
    dir_pdb2: "/u06"
    dir_pdb3: "/u07"
    dir_s3_golden_mount: "/golden_image"

    s3_bucket: "oracle-test-backups-terraform"
    s3_golden_bucket: "oracle-golden-images-terraform"

    passwords: "aoeuiAOEUI9"
    name_cdb: "TESTCDB"
    name_pdb1: "TEST1PDB"
    name_pdb2: "TEST2PDB"
    name_pdb3: "TEST3PDB"

  tasks:
    # ================== 1. PRZYGOTOWANIE OS & POWŁOKI ==================
    - name: Włączenie repozytorium EPEL
      yum:
        name: oracle-epel-release-el8
        state: present

    - name: Instalacja pakietów systemowych
      yum:
        name:
          - oracle-database-preinstall-19c
          - s3fs-fuse
          - lvm2
          - unzip
          - vim
          - tmux
        state: present

    - name: Wyłączenie SELinux
      block:
        - lineinfile:
            path: /etc/selinux/config
            regexp: '^SELINUX='
            line: SELINUX=permissive
        - shell: setenforce 0
          failed_when: false

    - name: Zmiana hasła użytkownika oracle
      user:
        name: oracle
        password: "{{ 'aoeuiAOEUI9' | password_hash('sha512') }}"

    - name: Konfiguracja tmux i bash dla oracle
      block:
        - copy:
            dest: /home/oracle/.tmux.conf
            owner: oracle
            group: oinstall
            content: |
              set -g mouse on
              set -g mode-keys vi
              bind h split-window -h -c "#{pane_current_path}"
              bind v split-window -v -c "#{pane_current_path}"
        - blockinfile:
            path: /home/oracle/.bashrc
            create: yes
            owner: oracle
            group: oinstall
            content: |
              set -o vi
              alias ll='ls -lah'
              alias l='ll'
              alias dbenv='export ORACLE_BASE={{ dir_oracle_base }}; export ORACLE_HOME={{ dir_db_home }}; export ORACLE_SID={{ name_cdb }}; export PATH=$ORACLE_HOME/bin:$PATH'
              dbenv
        - blockinfile:
            path: /home/oracle/.bash_profile
            create: yes
            owner: oracle
            group: oinstall
            content: |
              if [ -f ~/.bashrc ]; then . ~/.bashrc; fi

    # ================== 2. DYNAMICZNE MAPOWANIE DYSKÓW I LVM ==================
    - name: Zbieranie informacji o dyskach NVMe
      shell: lsblk -d -n -o NAME,SIZE | grep nvme
      register: nvme_disks
      changed_when: false

    - name: Pobranie największego dodatkowego dysku
      shell: lsblk -d -n -b -o NAME,SIZE | awk '$1 != "nvme0n1" && $2 > 20000000000 {print "/dev/"$1}' | head -n 1
      register: disk_30g
      changed_when: false

    - name: Pobranie dysków mniejszych niż 15GB
      shell: lsblk -d -n -b -o NAME,SIZE | awk '$1 != "nvme0n1" && $2 < 15000000000 {print "/dev/"$1}'
      register: disks_10g
      changed_when: false

    - name: Zdefiniowanie dynamicznej listy wolumenów LVM
      set_fact:
        lvm_volumes:
          - { device: "{{ disk_30g.stdout }}", vg: "u01vg", lv: "binlv", mount: "/u01" }
          - { device: "{{ disks_10g.stdout_lines[0] }}", vg: "u02vg", lv: "loglv", mount: "{{ dir_db_logs }}" }
          - { device: "{{ disks_10g.stdout_lines[1] }}", vg: "u03vg", lv: "mirrlv", mount: "{{ dir_db_mirr }}" }
          - { device: "{{ disks_10g.stdout_lines[2] }}", vg: "u04vg", lv: "pdb1lv", mount: "{{ dir_pdb1 }}" }
          - { device: "{{ disks_10g.stdout_lines[3] }}", vg: "u05vg", lv: "pdb2lv", mount: "{{ dir_pdb2 }}" }
          - { device: "{{ disks_10g.stdout_lines[4] }}", vg: "u06vg", lv: "pdb3lv", mount: "{{ dir_pdb3 }}" }

    - name: Utworzenie punktów LVM i montowanie
      block:
        - file: path="{{ item.mount }}" state=directory mode='0755'
          loop: "{{ lvm_volumes }}"
        - lvg: vg="{{ item.vg }}" pvs="{{ item.device }}"
          loop: "{{ lvm_volumes }}"
        - lvol: vg="{{ item.vg }}" lv="{{ item.lv }}" size=100%FREE shrink=no
          loop: "{{ lvm_volumes }}"
        - filesystem: fstype=xfs dev="/dev/{{ item.vg }}/{{ item.lv }}"
          loop: "{{ lvm_volumes }}"
        - mount: path="{{ item.mount }}" src="/dev/{{ item.vg }}/{{ item.lv }}" fstype=xfs opts=defaults state=mounted
          loop: "{{ lvm_volumes }}"

    - name: Konfiguracja katalogów pod bazę
      file:
        path: "{{ item }}"
        owner: oracle
        group: oinstall
        state: directory
        mode: '0775'
        recurse: yes
      loop:
        - "{{ dir_ora_inventory }}"
        - "{{ dir_db_logs }}/oradata"
        - "{{ dir_db_logs }}/fast_recovery_area"
        - "{{ dir_db_logs }}/backup_logs"
        - "{{ dir_u02_base }}/diag"
        - "{{ dir_u02_base }}/audit"
        - "{{ dir_pdb1 }}/oradata"
        - "{{ dir_pdb2 }}/oradata"
        - "{{ dir_pdb3 }}/oradata"

    - name: Pobranie systemowych ID
      command: id -{{ item }} oracle
      register: ids
      loop: ['u', 'g']
      changed_when: false

    - name: Montowanie S3 Golden i Backup
      block:
        - modprobe: name=fuse state=present
        - file: path="{{ item }}" state=directory owner=oracle group=oinstall mode='0775'
          loop: ["{{ s3_golden_mount }}", "{{ dir_db_backup }}"]
        - file: src=/usr/bin/s3fs dest=/sbin/mount.s3fs state=link
        - mount: path="{{ dir_db_backup }}" src="{{ s3_bucket }}" fstype=s3fs opts="_netdev,allow_other,iam_role=auto,url=https://s3.eu-central-1.amazonaws.com,endpoint=eu-central-1,uid={{ ids.results[0].stdout }},gid={{ ids.results[1].stdout }},umask=022,connect_timeout=5,retries=3" state=mounted
        - mount: path="{{ s3_golden_mount }}" src="{{ s3_golden_bucket }}" fstype=s3fs opts="_netdev,allow_other,iam_role=auto,url=https://s3.eu-central-1.amazonaws.com,endpoint=eu-central-1,uid={{ ids.results[0].stdout }},gid={{ ids.results[1].stdout }},umask=022,connect_timeout=5,retries=3" state=mounted

    # ================== 3. INSTALACJA RDBMS (SE2) ==================
    - name: Utworzenie struktury pod DB Home
      file:
        path: "{{ dir_db_home }}"
        owner: oracle
        group: oinstall
        state: directory
        mode: '0775'
        recurse: yes

    - name: Rozpakowanie binarek bazy danych
      unarchive:
        src: "{{ s3_golden_mount }}/LINUX.X64_193000_db_home.zip"
        dest: "{{ dir_db_home }}"
        remote_src: yes
        owner: oracle
        group: oinstall
      args:
        creates: "{{ dir_db_home }}/bin/sqlplus"

    - name: Cicha instalacja RDBMS (Software Only)
      become_user: oracle
      shell: |
        {{ dir_db_home }}/runInstaller -silent -waitforcompletion -ignorePrereqFailure \
        oracle.install.option=INSTALL_DB_SWONLY \
        UNIX_GROUP_NAME=oinstall \
        INVENTORY_LOCATION={{ dir_ora_inventory }} \
        ORACLE_HOME={{ dir_db_home }} \
        ORACLE_BASE={{ dir_oracle_base }} \
        oracle.install.db.InstallEdition=SE2 \
        oracle.install.db.OSDBA_GROUP=dba \
        oracle.install.db.OSOPER_GROUP=dba \
        oracle.install.db.OSBACKUPDBA_GROUP=dba \
        oracle.install.db.OSDGDBA_GROUP=dba \
        oracle.install.db.OSKMDBA_GROUP=dba \
        oracle.install.db.OSRACDBA_GROUP=dba
        RC=$?
        if [ $RC -eq 0 ] || [ $RC -eq 6 ]; then touch {{ dir_db_home }}/.ansible_db_installed; exit 0; else exit $RC; fi
      environment: { CV_ASSUME_DISTID: "OEL7.6" }
      args:
        creates: "{{ dir_db_home }}/.ansible_db_installed"
        executable: /bin/bash

    - name: Wykonanie skryptów root.sh
      shell: "{{ item }}"
      loop:
        - "{{ dir_ora_inventory }}/orainstRoot.sh"
        - "{{ dir_db_home }}/root.sh"
      args:
        creates: "/etc/oratab"

    # ================== 4. BAZA KONTENEROWA I 3x PDB ==================
    - name: Tworzenie instancji CDB
      become_user: oracle
      shell: |
        {{ dir_db_home }}/bin/dbca -silent -createDatabase -templateName General_Purpose.dbc \
        -gdbname {{ name_cdb }} -sid {{ name_cdb }} -responseFile NO_VALUE -characterSet AL32UTF8 \
        -sysPassword "{{ passwords }}" -systemPassword "{{ passwords}}" \
        -createAsContainerDatabase true -numberOfPDBs 0 \
        -databaseType MULTIPURPOSE -automaticMemoryManagement false -totalMemory 4096 \
        -storageType FS -datafileDestination {{ dir_db_logs }}/oradata \
        -recoveryAreaDestination {{ dir_db_logs }}/fast_recovery_area -recoveryAreaSize 4096
      environment:
        ORACLE_HOME: "{{ dir_db_home }}"
        ORACLE_SID: "{{ name_cdb }}"
      args:
        creates: "{{ dir_oracle_base }}/admin/{{ name_cdb }}/adump"

    - name: Utworzenie 3 instancji PDB
      become_user: oracle
      shell: |
        source ~/.bashrc
        export ORACLE_HOME={{ dir_db_home }}
        export ORACLE_SID={{ name_cdb }}
        export PATH=$ORACLE_HOME/bin:$PATH
        sqlplus -s / as sysdba <<EOF
        CREATE PLUGGABLE DATABASE {{ name_pdb1 }} ADMIN USER pdbadmin IDENTIFIED BY "{{ passwords }}"
          FILE_NAME_CONVERT=('{{ dir_db_logs }}/oradata/{{ name_cdb }}/pdbseed/', '{{ dir_pdb1 }}/oradata/{{ name_cdb }}/{{ name_pdb1 }}/');
        CREATE PLUGGABLE DATABASE {{ name_pdb2 }} ADMIN USER pdbadmin IDENTIFIED BY "{{ passwords }}"
          FILE_NAME_CONVERT=('{{ dir_db_logs }}/oradata/{{ name_cdb }}/pdbseed/', '{{ dir_pdb2 }}/oradata/{{ name_cdb }}/{{ name_pdb2 }}/');
        CREATE PLUGGABLE DATABASE {{ name_pdb3 }} ADMIN USER pdbadmin IDENTIFIED BY "{{ passwords }}"
          FILE_NAME_CONVERT=('{{ dir_db_logs }}/oradata/{{ name_cdb }}/pdbseed/', '{{ dir_pdb3 }}/oradata/{{ name_cdb }}/{{ name_pdb3 }}/');
        ALTER PLUGGABLE DATABASE ALL OPEN;
        ALTER PLUGGABLE DATABASE ALL SAVE STATE;
        EXIT;
        EOF
      args:
        executable: /bin/bash

    - name: Konfiguracja domyślnych parametrów RMAN
      become_user: oracle
      shell: |
        export ORACLE_HOME={{ dir_db_home }}
        export ORACLE_SID={{ name_cdb }}
        export PATH=$ORACLE_HOME/bin:$PATH
        mkdir -p {{ dir_db_logs }}/{{ name_cdb }}/rman_backups {{ dir_db_logs }}/{{ name_cdb }}/autobackup
        rman target / <<EOF
        CONFIGURE DEFAULT DEVICE TYPE TO DISK;
        CONFIGURE CHANNEL DEVICE TYPE DISK FORMAT '{{ dir_db_logs }}/{{ name_cdb }}/rman_backups/bkp_%U';
        CONFIGURE CONTROLFILE AUTOBACKUP ON;
        CONFIGURE CONTROLFILE AUTOBACKUP FORMAT FOR DEVICE TYPE DISK TO '{{ dir_db_logs }}/{{ name_cdb }}/autobackup/ctl_%F';
        CONFIGURE DEVICE TYPE DISK BACKUP TYPE TO COMPRESSED BACKUPSET;
        EXIT;
        EOF
      args:
        executable: /bin/bash
